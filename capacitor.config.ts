import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.app.lithuanianlearning',
  appName: 'Lithuanian Learning',
  webDir: 'dist'
};

export default config;
